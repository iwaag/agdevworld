// The one tint every image-backed room lays over its background
// (`project_room` p1 step 4). A settings background drawn at full strength
// competes with the portraits and the board panels in front of it — the
// braindump's own observation about every scene. Chosen by looking at the
// three rooms with their real backgrounds: 0.25 still let the Project Room's
// bright set fight the panels, 0.4 flattened the Arguing Room; 0.32 keeps
// the room recognisable and the text in front of it legible.
export const ROOM_TINT_COLOR = 0x0d0f14
export const ROOM_TINT_ALPHA = 0.32
